# Datasource properties (MySQL DB)
spring.datasource.url=jdbc:mysql://localhost:3306/engagetech?useSSL=false
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA properties
spring.jpa.database-platform=org.hibernate.dialect.MySQL5Dialect

#Tomcat properties
# Configure the server to run with SSL/TLS and using HTTPS
server.port=4884
server.ssl.key-store = classpath:tomcat.keystore
server.ssl.key-store-password = engage
server.ssl.key-password = engage
